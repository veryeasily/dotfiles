#!/bin/bash
SITE="$1"
if [[ -z $SITE ]]; then
  echo "Error: Devdocs command requires a search string!" 1>&2
  exit 128
fi

DDOC_PATH="http://devdocs.io/#q=${SITE}"

/mnt/c/Windows/System32/cmd.exe /c 'start chrome /new-window '"$DDOC_PATH" </dev/null >/dev/null 2>&1 &
